# Convex deployment the dashboard subscribes to — chat, notepad, and all
# observability go through this one URL (there is no eve server).
# Local dev:    http://127.0.0.1:3210
# Convex cloud: https://<name>.convex.cloud
VITE_CONVEX_URL=http://127.0.0.1:3210
